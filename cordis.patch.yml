# Bundle patch layer: the row loads the package's main entry as the host half
# (a plain Cordis plugin). No client bundle: memory is host-side behavior.
- insert:
    - id: dsh-hippocampus
      name: dsh-hippocampus
